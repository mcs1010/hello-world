package Devices;

public class commandDevice {
	private Device outputDevice;
	 public <T> void receiveInputSignal(T signal){
		 
	 }
	 
	 public <T> T sendOutputSignal(T signal){
		 T toReturn = signal;
		 
		 return toReturn;
	 }
}
